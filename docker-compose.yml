version: '3.8'
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
      # network: host
      # args:
      #  - http_proxy=http://127.0.0.1:10808
      #  - https_proxy=http://127.0.0.1:10808
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules # предотвращает перезапись папки модулей локальной папкой
    # environment:
    #  - CHOKIDAR_USEPOLLING=true # дополнительная настройка для Hot Reload
